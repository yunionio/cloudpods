get:
  summary: 获取指定Nat详情信息
  parameters:
    - $ref: "../parameters/natgateway.yaml#/natgatewayId"
  responses:
    200:
      description: Nat网关信息
      schema:
        $ref: "../schemas/natgateway.yaml#/NatGatewayResponse"
  tags:
    - natgateway
