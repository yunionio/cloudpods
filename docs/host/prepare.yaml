post:
  summary: 重新收集物理机信息
  parameters:
    - $ref: "../parameters/host.yaml#/hostId"
  responses:
    200:
      description: 宿主机信息
      schema:
        $ref: "../schemas/host.yaml#/HostResponse"
  tags:
    - host
