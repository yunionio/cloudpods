post:
  summary: 同步配置信息
  parameters: 
    - $ref: '../parameters/instance.yaml#/instanceId'
  responses:
    200:
      description: 实例信息
      schema:
        $ref: '../schemas/instance.yaml#/InstanceResponse'
  tags:
    - instance