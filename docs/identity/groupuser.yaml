put:
  summary: 将用户加入组
  parameters:
    - $ref: '../parameters/identity.yaml#/group_id'
    - $ref: '../parameters/identity.yaml#/user_id'
  responses:
    200:
      description: 成功
  tags:
    - users

delete:
  summary: 将用户从组中删除
  parameters:
    - $ref: '../parameters/identity.yaml#/group_id'
    - $ref: '../parameters/identity.yaml#/user_id'
  responses:
    200:
      description: 成功
  tags:
    - users
