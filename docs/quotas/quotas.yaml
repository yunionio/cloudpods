get:
  summary: 获得用户所在项目的配额
  parameters:
    - $ref: '../parameters/quota.yaml#/scope'
  responses:
    200:
      description: 配额信息
      schema:
        $ref: "../schemas/quota.yaml#/QuotaGetResponse"
  tags:
    - quota
