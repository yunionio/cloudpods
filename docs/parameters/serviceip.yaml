serviceipId:
  name: serviceipId
  required: true
  in: path
  type: string
  description: 服务IP名称或ID,建议使用ID

service_type:
  name: service_type
  in: query
  type: string
  enum: [dbinstance]
  description: 根据服务类型过滤资源

service_id:
  name: service_id
  in: query
  type: string
  description: 根据服务ID过滤资源