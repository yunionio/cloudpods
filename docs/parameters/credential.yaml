type:
  name: type
  type: string
  in: query
  example: totp
  description: 以type字段过滤列表结果, 例如totp, recovery_secret

user_id:
  name: user_id
  type: string
  in: query
  description: 以user_id过滤指定用户的credentials

